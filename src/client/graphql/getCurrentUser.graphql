query GetCurrentUser {
  currentUser {
    id
    name
    email
    kois {
      variety
      id
      birthDate
      breeder
      bloodline
      skinType
      sex
      youtube
      updates {
        image
        length
        date
        id
      }
    }
  }
}
