query GetCurrentUser {
  currentUser {
    id
    name
    email
    projects {
      id
      name
      slug
      paidPlan
    }
    kois {
      variety
      id
      birthDate
      breeder
      bloodline
      skinType
      sex
      youtube
    }
  }
}
